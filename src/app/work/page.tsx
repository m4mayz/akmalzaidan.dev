import { PlaceholderPage } from "@/components/placeholder-page";

export default function WorkPage() {
    return (
        <PlaceholderPage
            description="A dedicated archive for selected product, web, and visual direction work is being prepared."
            eyebrow="Work"
            title="Selected work"
        />
    );
}
